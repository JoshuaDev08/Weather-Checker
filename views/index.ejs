<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles/main.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <title>Weather Search</title>
</head>
<body>
    <div class="container">
        <div class="search-bar">
            <form action="/" method="post">
                <div class="box">
                    <i class='bx bxs-map'></i>
                    <input type="number" placeholder="Enter your Zipcode" id="zipCodeInput" name="zipcode" required>
                    <button class="bx bx-search" type="submit" value="post"></button>
                </div>
            </form>
        </div>

        <div class="weather-box">
            <div class="box">
                <div>
                <% if (weatherData) { %>
                    <img src="<%= weatherData.icon %>"  alt="Weather Icon">
                    <h2 class="weather-name"><%= weatherData.main %></h2>
                    <h2 class="weather-degree"><%= weatherData.temperature %> <span>°C</span></h2>
                    <p class="weather-descip"><%= weatherData.description %></p>
                <% } else { %>
                    <h2 class="weather-name">Weather Data Unavailable</h2>
                <% } %>
                </div>
            </div>
        </div>

        <div class="weather-main">
            <div class="box">
                <div class="cityname">
                    <i class='bx bxs-city'></i>
                    <p>City: <%= weatherData ? weatherData.city : "N/A" %></p>
                </div>
                <div class="humidity">
                    <i class='bx bx-water'></i>
                    <p>Humidity: <%= weatherData ? weatherData.humidity : "N/A" %> %</p>
                </div>
            </div>
            <div class="box">
                <div class="wind">
                    <i class='bx bx-wind'></i>
                    <p>Wind: <%= weatherData ? weatherData.wind : "N/A" %> km/h</p>
                </div>
                <div class="pressure">
                    <i class='bx bx-current-location'></i>
                    <p>Pressure: <%= weatherData ? weatherData.pressure : "N/A" %> hPa</p>
                </div>
            </div>
        </div>

        <div class="error-container">
            <div class="error">
            <% if (error) { %>
                <img src="images/404.png" alt="">
                <p style="color: red;"><%= error %></p>
            <% } %>
            </div>
        </div>  
    </div>

</body>
</html>
